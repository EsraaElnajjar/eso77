
<br><br><br>

<div style="background-color:rgba(255, 99, 71, 0.5);" class="container">
    <br>
<h1 >Edit {{subject.name}}</h1>
<form action="/subjects/{{subject._id}}?_method=PUT" method="post">
    <div>
        <label for="">Name </label>
        <input type="text" class="form-control" name="name" value="{{subject.name}}">
    </div>
    <br>
     <div>
        <label for="">Code  </label>
        <input type="text" class="form-control" name="code" value="{{subject.code}}">
    </div>
    <br>
    <div>
        <label for="">Department </label>
        <select name="department" id=" " class="form-select">
            {{#each departments}}

               <option value="{{_id}}">{{name}}</option>


            {{/each}}
            
        </select>
    </div>
    <br>
    
<div><br><br>
    <button class="btn btn-danger btn-lg btn-block" type ="submit"> Save</button>
</div>
<br>
</form>
</div>
<div style="background-color:rgba(255, 99, 71, 0.5);" class="container">
    <br> 